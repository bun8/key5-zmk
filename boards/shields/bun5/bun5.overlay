#include <dt-bindings/zmk/keys.h>

/ {
    kscan: kscan {
        compatible = "zmk,kscan-gpio-direct";

        input-gpios =
            <&xiao_d 0 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
    };

    chosen {
        zmk,kscan = &kscan;
    };
};